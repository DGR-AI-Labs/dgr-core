# PROD-001 non-author cross-model review

**Verdict:** `PASS WITH NON-BLOCKING FINDINGS — CROSS-MODEL GATE SATISFIED`

## Reviewer separation

- **Model/vendor:** Anthropic Claude Opus 5. Container tooling used for independent verification: `sha256sum`, `git` (blob/tree/EOL-insensitive diff), `python3` (JSON/SARIF/lockfile parsing), `node` v22.22.2.
- **Review timestamp:** `2026-09-03T16:47:37Z` (start of inspection; completed in the same session).
- **Non-author attestation:** The PROD-001 implementation, the extraction evidence, and this bundle were prepared by OpenAI Codex. This reviewer is a different vendor and model family, authored none of the reviewed source, manifests, ledgers, logs, or evidence records, and did not participate in the extraction. Disclosure for completeness: this reviewer authored the PROD-000 cross-model review and its addendum, which are referenced by the reviewed governance documents but are **not** part of this bundle and were not re-reviewed here; the PROD-001 source and evidence under review are entirely Codex-authored.
- **Materials actually inspected:** every file in the bundle was digest-verified; substantively read or parsed: `README-FIRST.md`, `MANIFEST.md`, `review/PROMPT.md`, `metadata/baseline-to-reviewed.diff` (all 22 changed paths), `metadata/change-name-status.txt`, `change-numstat.txt`, `rename-summary.txt`, `t0-byte-identity.sha256`, `shared-definitions.sha256` plus all six region files, `commit-tree-identities.txt`, `verification-summary.md`, `baseline-files.sha256`, `reviewed-files.sha256`, `analyzer-files.sha256`, `external-authorities.sha256`, `cargo-metadata.json`, `cargo-lock-inventory.txt`, `local-cargo-test.txt`, `test-list.txt`, `local-clippy.txt`, `ignored-set-guard.txt`, `structural-check.txt`, `ci-job-names.before/after.txt`, `tests-and-guards-diff.empty`; both snapshots in full (38 baseline / 41 reviewed files), including both `Cargo.toml`s, both `lib.rs`s, all nine relocated enforcement files, the retained harness sources and tests, `README.md`, `T0-AUTHORS.md`, `tests/bypass-rust/T0-BOUNDARY.md`, `qa/prod-001-extraction-review-input.md`, `.github/workflows/ci.yml`, `.gitignore`, `src/gate.mjs`; and all twelve `analyzers/` artifacts including the complete GitHub job logs for the Rust, structural, Semgrep, CodeQL and cargo-deny jobs, `github-codeql.sarif`, `github-codeql-analysis.json`, `github-pr-91.json`, `github-run-artifacts.json`, `local-semgrep.json`, `local-cargo-deny.txt`.

## Integrity

| Check | Result | Evidence |
|---|---|---|
| Complete manifest | **PASS** | `sha256sum -c MANIFEST.sha256` → exit 0, **122 of 122 `OK`, 0 `FAILED`**. 122 files present excluding the manifest; zero files present-but-unlisted and zero listed-but-absent. `MANIFEST.md` is itself covered by the manifest, as claimed. Bundle archive matches its sidecar: `b241800339e803d32b8656c782b8c87953c4cd60d95a6994a1e9baf2e8a66cc5`. |
| T0 byte ledger | **PASS** | `sha256sum -c metadata/t0-byte-identity.sha256` → **all 18 paths resolve and verify `OK`**. Nine before/after pairs, each pair carrying one identical digest across `dgr-core/baseline/tests/bypass-rust/src/` and `dgr-core/reviewed/src/`. |
| Shared-definition ledger | **PASS** | `sha256sum -c metadata/shared-definitions.sha256` → **all six regions `OK`**. Independently anchored (not merely self-consistent): each `*.before.rs` region reproduces from `dgr-core/baseline/tests/bypass-rust/src/lib.rs` at lines 22–29 / 42–52 / 54–59, and each `*.after.rs` from `dgr-core/reviewed/src/lib.rs` at lines 17–24 / 26–36 / 38–43, exactly as the public record states. |
| Full patch digest | **PASS** | `sha256sum metadata/baseline-to-reviewed.diff` → `c896f1dc3e6eeb6a447cce7a4ad71878a2ddd5bfc8ce0d005f18d92c9a24ba70`, matching the required value. |
| Identity and selection limits | **PASS, with the stated limit** | `metadata/commit-tree-identities.txt` records baseline `8318f61e…`/tree `227949bf…`, relocation `91589759…`/tree `3c1295c4…`, reviewed head `f1d17087…`/tree `350d1f1b…`, merge ref `d45374fb…`, run `33768827329`, analysis `1719130860`. Head identity is corroborated by `analyzers/github-pr-91.json` (`headRefOid f1d17087…`, `state OPEN`, `isDraft true`) and by `analyzers/github-rust-format-build-test.log:117`: `HEAD is now at d45374f Merge f1d17087d140b41750c1aeca032916bb4d2d90ae into 8318f61eadf689f9b8a72f673cc68cd083dc7831`. As the manifest requires, **no tree reconstruction is claimed**: the two snapshots are selected sets (38 and 41 files, both inventories verifying 38/38 and 41/41), so commit-to-tree statements remain external Git identity claims. |
| Sensitive-content exclusions | **PASS** | No `.git` directory, no nested archive (`find` for `*.zip/tar/tgz/gz/7z` → empty), no credential or private-key material (a `-----BEGIN` probe matched zero times in `github-codeql.sarif`; the earlier grep hit was a filename-level false positive, confirmed by byte count 0). No internal ADR body: probes for the Amendment-B section headings and body phrases (`Exact implementation scope`, `Load-bearing sequence`, `silently blended`, `Supervised implementation controls`) matched **nothing**. `dgr-backlog` appears only as the word in `MANIFEST.md`'s exclusion statement; `ADR-14` appears only as an identifier in `review/PROMPT.md`, the patch, and `qa/prod-001-extraction-review-input.md`. `metadata/external-authorities.sha256` carries three identity digests only; **this review makes no claim to have read the internal authority texts, and no authority-conformance conclusion is drawn from them.** |

Integrity did not fail; source conclusions proceed.

## Extraction matrix

| # | Verdict | Evidence and reasoning |
|---|---|---|
| 1 | `YES` | All nine are byte-identical moves. `metadata/change-name-status.txt:8-16` classifies each as `R100`; `metadata/rename-summary.txt:4-12` shows `rename {tests/bypass-rust/src => src}/… (100%)`; `metadata/change-numstat.txt:8-16` shows `0 0` added/removed for every one. Independently confirmed by the 18-path digest ledger (all `OK`), and by the patch itself, which contains only `similarity index 100% / rename from / rename to` headers for these paths and no content hunks. |
| 2 | `YES` | `RequiredOutcome`, `ProposedAction` and `DecisionContext` moved as complete source regions including doc comment, `#[derive]` and full body. `metadata/shared-definitions.sha256` verifies each before/after pair to one digest (`96e1592c…`, `39671820…`, `0e5ee3bc…`), and this reviewer anchored every region to real source at `dgr-core/baseline/tests/bypass-rust/src/lib.rs:22-29,42-52,54-59` → `dgr-core/reviewed/src/lib.rs:17-24,26-36,38-43`. Exactly one definition site each remains, in the root library; a repository-wide grep found no duplicate definition in the harness. |
| 3 | `YES` | No enforcement expression changed, and nothing is hidden in wiring. (a) File bytes are identical (Q1). (b) The nine files' internal references are closed over the new crate root: the complete set of `crate::` paths inside `dgr-core/reviewed/src/founder_*.rs` is `crate::RequiredOutcome`, `crate::{DecisionContext, ProposedAction, RequiredOutcome}`, `crate::{ProposedAction, RequiredOutcome}` and the nine `crate::founder_*` modules — every one of which `dgr-core/reviewed/src/lib.rs:7-15,17-43` provides, with the three types byte-identical to their originals. No reference to `attack_by_id`, `ATTACK_SET`, `fixtures`, `before_tool_call`, observations or probes survives. (c) No `#[cfg]` or `feature =` gating exists anywhere in the nine files. (d) No `[patch]`, `[replace]`, `.cargo/config*`, or vendor directory exists in either manifest or the reviewed snapshot. (e) Re-export wiring is name-preserving: `dgr-core/reviewed/tests/bypass-rust/src/lib.rs:12-17` re-exports the three types and the nine modules under their original identifiers, which is why `tests/bypass-rust/src/before_tool_call.rs`, `fixtures.rs` and all seven test files required **zero** edits (absent from `change-name-status.txt`; `metadata/tests-and-guards-diff.empty` is genuinely 0 bytes). |
| 4 | `YES` | `dgr-core/reviewed/Cargo.toml:1-9` defines a single `[package] name = "dgr-core"`, `version = "0.1.0"` (0.x), `edition = "2024"`, `license = "Apache-2.0"`, `publish = false`, with `[lib] path = "src/lib.rs"`. `metadata/cargo-metadata.json` resolves `dgr-core 0.1.0` with `publish: []`, i.e. publication disallowed. `publish = false` blocks crates.io while leaving Git-dependency consumption unaffected, which is exactly the stated intent (`README.md:19-20`: "Git dependency only and not published to crates.io"). |
| 5 | `YES` | `dgr-core/reviewed/tests/bypass-rust/Cargo.toml:13` declares `dgr-core = { version = "=0.1.0", path = "../.." }` — an exact-version path dependency, corroborated by `cargo-metadata.json` (`dep dgr-core =0.1.0 … path`). One workspace: `Cargo.toml:16-18` `[workspace] members = ["tests/bypass-rust"] resolver = "3"`, and `cargo-metadata.json` lists exactly two members. One lockfile: `tests/bypass-rust/Cargo.lock` is deleted (`change-name-status.txt:19`, `-564` lines) and root `Cargo.lock` added (`+573`); `metadata/cargo-lock-inventory.txt` lists only `dgr-core/reviewed/Cargo.lock`, and the reviewed snapshot contains no other `Cargo.lock`. |
| 6 | `YES` | `dgr-core/reviewed/src/lib.rs` is 43 lines total and contains **only** the nine `pub mod` declarations (7–15) and the three shared definitions (17–43). Nothing else is exported at the crate root. All nine modules are genuinely required by the conformance consumer: eight are referenced directly by test files via `dgr_core_bypass_harness::founder_*`, and `founder_before_tool_call_floor` is required by the retained adapter at `tests/bypass-rust/src/before_tool_call.rs`. **No missing required export** — the harness compiles and all 52 tests pass with zero test-source edits. **No unnecessarily public item was found** beyond one bounded observation: the modules carry eleven `pub const CONFORMANCE_*` mirrors (one in `founder_approval_timeout.rs:16`, eight in `founder_authored_guard.rs:35-48`, two in `founder_token_verification.rs`), all marked `#[doc(hidden)]`. These are unchanged from the reviewed PROD-000 state, not introduced by PROD-001, and are recorded as finding **F-4**. No public item in the root modules references a fixture, attack, probe, observation, registry or test concept. |
| 7 | `YES` | Versions: comparing the deleted harness lockfile against the new workspace lockfile, the package sets are **62 → 63 with the single addition of `dgr-core 0.1.0`, zero removals, and zero version changes on any third-party package**. Features: `rusqlite = { version = "=0.40.2", features = ["bundled"] }` moved verbatim to root `Cargo.toml:13`; `base64ct = { version = "=1.8.3", features = ["alloc"] }` is retained by the harness at `tests/bypass-rust/Cargo.toml:12`; `ed25519-dalek =2.2.0` and `sha2 =0.10.9` appear in both packages. `cargo-metadata.json` confirms the resolved feature requirements (`rusqlite ['bundled']` on root, `base64ct ['alloc']` on harness). The direct `rusqlite` removal from the harness is correct and not a coverage loss: `rusqlite` is referenced only by `founder_s2_approval_store.rs` and `founder_s2_consumption_store.rs`, both now in the root crate, and no retained harness file references it. All three remaining harness dependencies are still used (`base64ct`, `ed25519_dalek`, `sha2` all appear in `val_002_fixtures.rs`/`val_004_fixtures.rs`), so the split leaves no unused pin. |
| 8 | `YES` | The retained harness keeps `before_tool_call.rs` (adapter, `BeforeToolCallObservation`, `EffectfulToolProbe`), `fixtures.rs` (probes), `val_002_fixtures.rs`, `val_004_fixtures.rs`, the registry (`HarnessTarget`, `AttackCase`, `attack_by_id`, `ATTACK_SET` in its 235-line `lib.rs`) and all seven integration test files. `dgr-core/reviewed/tests/bypass-rust/src/` contains exactly `before_tool_call.rs`, `fixtures.rs`, `lib.rs`, `val_002_fixtures.rs`, `val_004_fixtures.rs` — none of the nine. Conversely `dgr-core/reviewed/src/` contains only the nine files plus `lib.rs` (and `gate.mjs`, finding **F-3**), and `src/lib.rs` exports nothing from the harness. The root crate has no `[dev-dependencies]` and no test targets, so no harness code is compiled into it. |
| 9 | `YES` | Test and guard sources are unchanged: `metadata/tests-and-guards-diff.empty` is 0 bytes, `change-name-status.txt` lists no file under `tests/bypass-rust/tests/` or `scripts/`, and `attack_set.rs`, `core_004_conformance.rs`, `fixtures.rs`, `before_tool_call.rs` and both guard scripts are absent from the change set. The 52/5 figure is established by the required GitHub job: summing the nine `test result:` lines in `analyzers/github-rust-format-build-test.log` (two lib-unittest targets at 0 tests plus seven integration targets) gives exactly **52 passed, 0 failed, 5 ignored**. `metadata/local-cargo-test.txt` independently sums to 52/0/5 across eight targets; the one-target difference is finding **F-5**. |
| 10 | `YES` | `analyzers/github-rust-format-build-test.log:427-428` records `ignored-set guard passed: atk_04_missing_justification, atk_05_ambiguous_evidence, atk_12_revoked_credential, atk_14_cross_tenant_use, atk_15_deploy_role_data_access` and `required active conformance guard passed: atk_06_sequence_is_escalated_then_registry_derived_timeout_block`, reproduced in `metadata/ignored-set-guard.txt`. The five ignored tests appear with `... ignored` in the `attack_set` target and nowhere else; `atk_06_sequence_is_escalated_then_registry_derived_timeout_block` appears as `... ok` in the `core_004_conformance` target and is listed at `metadata/test-list.txt:48`. |
| 11 | `YES` | Job names are byte-identical: `metadata/ci-job-names.before.txt` and `.after.txt` both contain the same five names, and the `ci.yml` diff touches only `run:` bodies and step display names — the five `name:` job lines are untouched, preserving `Structural / governance check` and `Rust format / build / test`. Workspace-root coverage of both packages is not merely configured but **observed**: `github-rust-format-build-test.log:121-129,220-221,251-252` shows `cargo fmt --manifest-path Cargo.toml --all -- --check`, `cargo build --workspace --all-targets --locked`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `cargo test --workspace --all-targets --locked`, and lines 217–218 show `Compiling dgr-core v0.1.0` **and** `Compiling dgr-core-bypass-harness v0.0.0`. The moved root Rust files are covered by the Semgrep step's widened selector `git ls-files -- '*.rs'` (`ci.yml`), which enumerated **22** targets (`github-semgrep.log:469,475`) = 10 root (nine moved + new `src/lib.rs`) + 12 retained harness files, matching `local-semgrep.json`'s scanned set exactly. CodeQL builds `--workspace` and cargo-deny now points at the root manifest. |
| 12 | `YES` | Documentation is truthful about provenance, locations, proof limits and the absence of runtime guarantees. `src/lib.rs:1-5` states the crate holds the reviewed T0 units and that "Extraction changes distributability, not the bounded isolation proof. Runtime interception and non-bypassability remain unproven." `T0-AUTHORS.md` repaths the nine founder-owned entries to `src/…` while leaving the two genuinely harness-resident rows (`tests/bypass-rust/src/before_tool_call.rs` historical source and adapter behavior) unchanged, and adds a PROD-001 section stating the move is byte-identical relocation and wiring that "does not become authorship of any moved enforcement body." `T0-BOUNDARY.md` restates the harness as the retained T3 consumer, repaths the floor, adds a PROD-001 extraction boundary forbidding any change to enforcement expressions, conformance expectations, active/ignored sets, denial signals, deadlines, store operations or bounded claims, and retains every non-claim (route-around, never-firing hook, missing plugin, operator bypass reserved to RUNTIME-003/004; the bounded CORE-004 claim; "must never become an alternate production enforcement path"). `README.md` adds the root library with "Git dependency only and not published to crates.io" and keeps "not a deployed runtime gate". `qa/prod-001-extraction-review-input.md:7-8,§7` states "This is evidence for review, not approval" and lists the non-claims verbatim. No document overstates the result. |
| 13 | `YES` | The four-way distinction is preserved explicitly. `T0-AUTHORS.md` PROD-001 section: founder-authored source moved byte-identically "remains founder-authored source"; the PROD-000 agent-authored floor "remains agent-authored T0 with its recorded founder-source provenance for transformed regions"; PROD-000 agent-transformed regions "retain their existing classification"; PROD-001 "is agent-assisted relocation and wiring… pending byte-identity review and founder merge"; and founder supervision, review, approval and merge "do not convert agent-authored or agent-transformed bytes into founder-authored bytes." `qa/prod-001-extraction-review-input.md:§6` repeats the same five statements. The `{AGENT-AUTHORS}` and `{FOUNDER-AUTHORS}` in-source markers travel unchanged with the moved files. One bounded gap in the *evidence* rather than the classification is recorded as **F-8**. |
| 14 | `NO` | No unreviewed route was found. (a) The harness's only path to enforcement is `dgr-core = { version = "=0.1.0", path = "../.." }`; a path dependency always resolves locally, and `publish = false` means no registry copy of `dgr-core` can exist to resolve instead. (b) There is no `[patch]`, `[replace]`, `.cargo/config*`, or vendor directory in either manifest or the reviewed snapshot. (c) One workspace, one lockfile, and the lockfile's `dgr-core` entry carries only the three expected dependencies. (d) No `#[cfg]` or feature gating exists in the nine files, so no alternate body can be selected at compile time. (e) Closure of the tracked Rust set is independently established, not assumed: the CI Semgrep step ran `git ls-files -- '*.rs'` against the merge-ref checkout and enumerated exactly **22** files, identical to the 22 in `local-semgrep.json` (10 root + 12 harness), so no third tracked copy of any enforcement file exists in the repository at the reviewed content. (f) No duplicate definition of the three shared types survives in the harness. The residual limit is that the snapshots are selected, so absence of *untracked* or non-Rust alternate copies is not establishable from the bundle; item (e) closes the tracked-Rust case, which is the one that can affect what compiles. |

## Analyzer matrix

| Evidence leg | Result | Findings, diagnostics, and limitations |
|---|---|---|
| Semgrep | **Verified as reported** | `local-semgrep.json`: version `1.173.0`, `results` = 1, `errors` = 0, `paths.scanned` = 22. `github-semgrep.log:469` `Semgrep tracked Rust targets: 22`; `:475-476` `Scanning 22 files with 11 Code rules` / `11 rust rules`; `:503-506` `Targets scanned: 22`, `Parsed lines: ~100.0%`, `Ran 11 rules on 22 files: 1 finding`; `:507` `Process completed with exit code 1`. The single result is `rust.lang.security.temp-dir.temp-dir` at `tests/bypass-rust/tests/consumption_store.rs:19`, severity `INFO` in JSON, promoted to a failure only by the informational job's `--error`. **Relationship to the changed footprint: none.** `consumption_store.rs` does not appear in `change-name-status.txt`; it is a pre-existing conformance-test helper, unmodified by PR #91. The 22-file set covers all nine relocated files and the new `src/lib.rs`, so the moved code was fully in Semgrep scope. Undispositioned; no risk judgment made here. |
| CodeQL security results | **Verified as reported, with an important scoping qualification** | `github-codeql-analysis.json`: tool `CodeQL 2.26.4`, `rules_count: 27`, `results_count: 0`, `error: ""`, `warning: ""`, `ref refs/pull/91/merge`, `commit_sha d45374fb…`. Query pack `codeql/rust-queries` `0.1.41` (`github-codeql.log:1199`); CLI 2.26.4 (`:148-149`). The SARIF corroborates: 0 results, 27 rule objects across driver plus extensions (`codeql/rust-queries 0.1.41+1d123a2c…`, `codeql/rust-all 0.2.20`, `codeql/threat-models 1.0.56`), and `versionControlProvenance` self-binding the run to `d45374fb…` on `refs/pull/91/merge`. **Qualification (finding F-1):** this run applied PR diff-range filtering — `:575-576` `Computing PR diff ranges… Persisted 30 diff range(s) across 11 file(s)`, `:1133-1134` diff-range extension pack generated, `:1196` `run-queries … --extension-packs=codeql-action/pr-diff-range`, and the SARIF lists `codeql-action/pr-diff-range 0.0.0` as an extension. Because the nine enforcement files are 100% renames with zero changed lines, the only Rust files carrying diff ranges are `src/lib.rs` and `tests/bypass-rust/src/lib.rs` (11 files with ranges = the 12 non-rename changed paths minus the deleted lockfile). `results_count: 0` therefore means "no alerts within the PR's changed line ranges", **not** "zero alerts in the relocated enforcement code". Byte identity independently establishes the code is unchanged, so this is a claim-scoping issue rather than a regression risk. |
| CodeQL extraction/diagnostics | **Verified as reported; second raw message not enumerable** | `github-codeql.log:1447-1450` metric table: `Total number of Rust files that were extracted without error | 22` and `Total number of Rust files that were extracted with errors | 1`. `:1439` `Found 2 raw diagnostic messages`; `:1440` `Processed diagnostic messages (removed 0 due to limits, created 0 summary diagnostics for status page)`. The generated-file warning is visible at `:1175`: `WARN /home/runner/work/dgr-core/dgr-core/target/debug/build/libsqlite3-sys-cd598373a1da9e91/out/bindgen.rs:1:1: semantic analyzer unavailable (not included in files loaded from manifest): macro expansion will be skipped.` **Processed-SARIF limitation assessed and confirmed by inspection:** `github-codeql.sarif` run keys are exactly `automationDetails, conversion, properties, results, tool, versionControlProvenance` — it has **no `artifacts` and no `invocations`**, and the strings `toolExecutionNotifications`, `toolConfigurationNotifications`, `bindgen` and `diagnostic` do not occur anywhere in it. `github-run-artifacts.json` is `{"artifacts":[],"total_count":0}`, so no raw artifact exists to fall back on. **Decision (PROMPT §3.3): this does not block the cross-model gate.** The metric table bounds the diagnostics to a single file, that file is a generated dependency artifact under `target/`, all 22 tracked Rust files extracted without error, and no diagnostic can therefore attach to any relocated enforcement file. Not enumerating the second message leaves the diagnostic set incompletely described, which is a specifically bounded founder-gate item (F-1's companion action: upload the raw SARIF/diagnostic artifact so both messages are enumerable at the founder gate). |
| Tooling deprecations | **Bounded non-blockers; one with a deadline** | `github-codeql.log:136` `##[warning]CodeQL Action v3 will be deprecated in December 2026. Please update all occurrences of the CodeQL Action in your workflow files to v4.` → **bounded non-blocker with a December 2026 deadline**; not a current failure and not an in-scope correction, because changing action versions would alter workflow behavior, which the PROD-001 extraction boundary in `T0-BOUNDARY.md` and the reviewed scope explicitly exclude. Track it as a separate wiring change. `github-codeql.log:34,124,1104` and `github-cargo-deny.log:286` `Node.js 20 is deprecated… actions/checkout@v4 … being forced to run on Node.js 24` → **informational, not a blocker**: the jobs already ran on Node 24 and all required contexts passed. Neither diagnostic is a source finding, and neither touches a changed PROD-001 file. |
| cargo-deny | **Verified as reported; note identity not established** | GitHub: `github-cargo-deny.log:266-269` → `advisories ok: 0 errors, 0 warnings, 0 notes`; `bans ok: 0/0/2 notes`; `licenses ok: 0/0/53 notes`; `sources ok: 0/0/0`. Local: `analyzers/local-cargo-deny.txt` → identical except `licenses … 55 notes`. Both use cargo-deny **0.20.2** (`github-cargo-deny.log:82,119` shows the action image fetching `cargo-deny-0.20.2`), so the difference is not a tool-version difference. **Explanation without invention (finding F-2):** the two invocations differ in an evidenced way. CI ran `--manifest-path Cargo.toml --config deny.toml --locked check --show-stats` with **no `--workspace`** (`github-cargo-deny.log:237`, matching `ci.yml`'s `arguments`/`command-arguments`), whereas `qa/prod-001-extraction-review-input.md:113` records the local invocation as including `--workspace`. A different crate-graph scope is a sufficient mechanism for a different license-note count, but **the bundle does not prove causation**: neither run enumerated notes, and `local-cargo-deny.txt` records no command line, so **exact note content and identity are NOT established**. The two bans notes and all license notes remain undispositioned. |
| Results touching changed files | **None** | Semgrep's one result is in unchanged `tests/bypass-rust/tests/consumption_store.rs`. CodeQL uploaded zero results, and its one extraction diagnostic concerns a generated `target/` dependency file that is not tracked and not part of the change set. cargo-deny reports notes only, with no per-file locus. **No analyzer result or diagnostic touches any changed PROD-001 file.** This is a locational observation, not a risk disposition; each item's recommended bounded founder action is listed in Findings. |

## Provenance and scope

Classifications and non-claims are accurate. The bundle and the reviewed documents consistently hold four separate categories: founder-authored source that moved byte-identically and remains founder-authored; PROD-000 agent-authored and agent-transformed T0 that retains its existing classification and its recorded founder-source provenance; PROD-001 as agent-assisted relocation and wiring that confers no authorship over any moved enforcement body; and founder review, which is recorded as pending and is nowhere treated as complete. `T0-AUTHORS.md` and `qa/prod-001-extraction-review-input.md:§6` both state that founder supervision, review, approval and merge do not convert agent bytes into founder bytes. The Amendment-B-mandated `founder_` filename prefix is not used as an authorship claim; the `{AGENT-AUTHORS}` marker at the head of `src/founder_before_tool_call_floor.rs` and the ownership record continue to control.

The scope guardrail holds. Every reviewed document confines the result to package separation and preserved isolation-harness behavior, and explicitly disclaims runtime hook installation, complete interception, agent non-bypassability, same-process store or key protection, deployed fail-closed behavior, and any ADR-14 runtime-integration claim; `qa/prod-001-extraction-review-input.md:§7` adds that RUNTIME-003/004 remain inactive until PROD-001 is founder-reviewed and merged. This review found no document making a broader claim.

**The strongest conclusion this review supports, and the only one it asserts:** PR #91 at head `f1d17087…` separates a distributable root `dgr-core` library from its retained conformance harness while preserving the reviewed isolation-harness behavior and source provenance. Nothing here establishes deployed interception, hook installation, complete route coverage, agent non-bypassability, same-process state or key integrity, or any ADR-14 runtime finding.

## Findings

| ID | Severity | Evidence | Impact | Required action |
|---|---|---|---|---|
| F-1 | `NON-BLOCKING` | `analyzers/github-codeql.log:575-576,1133-1134,1196`; `analyzers/github-codeql.sarif` extension `codeql-action/pr-diff-range 0.0.0`; the nine files are `R100` with `0 0` numstat, so only `src/lib.rs` and `tests/bypass-rust/src/lib.rs` carry Rust diff ranges | `results_count: 0` is scoped to the PR's changed line ranges, so the CodeQL leg contributes essentially no security-query coverage of the nine relocated enforcement bodies on this run. A reader could misread it as a clean bill for the new library. No regression risk, since byte identity independently establishes the code is unchanged | State the diff-range scoping explicitly in `metadata/verification-summary.md` and in the founder record; before any external consumption of the crate, run one full-database CodeQL analysis of the root library (default-branch run or an equivalent non-diff-range invocation) and disposition its results |
| F-2 | `NON-BLOCKING` | `analyzers/github-cargo-deny.log:237` (CI invocation has no `--workspace`) and `:266-269` (53 notes); `analyzers/local-cargo-deny.txt` (55 notes, no command line recorded); `qa/prod-001-extraction-review-input.md:113` (local invocation includes `--workspace`); same tool version 0.20.2 at `github-cargo-deny.log:82` | The 53-vs-55 gap has an evidenced candidate mechanism (different crate-graph scope) but no proof, and note identity is not established on either side, so the licence posture of the split workspace is not fully characterised | Re-run both invocations with `--format json`, diff the note sets, and record which scope is authoritative; add the command line to the local artifact header. Do not treat either count as dispositioned until then |
| F-3 | `NON-BLOCKING` | `dgr-core/reviewed/Cargo.toml:8-9` sets `[lib] path = "src/lib.rs"`; `dgr-core/reviewed/src/gate.mjs` is present, byte-identical to baseline (`ac190e80216a6f4b9abf58e0fff5497c3b1d2816d2c1f7604342692cfbdd0191`) and unchanged by this PR | The deliberately failing Phase-0 JavaScript stub now sits inside the distributable crate's source directory. It cannot be compiled or reached by the Rust build and `publish = false`, so there is no execution risk; the risk is provenance and perception for a crate positioned as the distributable enforcement core, and a consumer browsing `src/` finds a "FAILING STUB" beside the enforcement modules | Relocate `gate.mjs` (for example under `tests/bypass/` or `legacy/`) in a separate wiring-only change, or record a founder-accepted note in `README.md` that root `src/` intentionally hosts both the Rust library and the legacy scaffold |
| F-4 | `NON-BLOCKING` | `dgr-core/reviewed/src/founder_approval_timeout.rs:15-16`, `founder_authored_guard.rs:34-48`, `founder_token_verification.rs` — eleven `pub const CONFORMANCE_*` items, all `#[doc(hidden)]` | The distributable library's public API includes conformance-only mirrors (the ATK-06 timeout outcome, lifetime/skew/threshold/approval-window constants, and a registered test public key). They are hidden from documentation but reachable by any Git-dependency consumer. Unchanged from the reviewed PROD-000 state, not introduced by PROD-001 | Founder decision at the PROD-000 public-surface disposition: either accept them as `#[doc(hidden)]` conformance surface with a recorded rationale, or gate them behind a `conformance` cargo feature before any external consumption. Not an in-scope PROD-001 change |
| F-5 | `NON-BLOCKING` | `metadata/local-cargo-test.txt` shows eight test targets with a single lib-unittest binary (`dgr_core_bypass_harness-f33e88c5…`); `analyzers/github-rust-format-build-test.log:256,262` shows nine targets including `dgr_core-60f2e5ad…` | The local artifact is harness-scoped, not the workspace-scoped command CI now runs. Both total 52 passed / 5 ignored, so the figure is unaffected, but the local evidence does not exercise the CI command | Regenerate `local-cargo-test.txt` with `cargo test --workspace --all-targets --locked`, or label it explicitly as a harness-scoped run |
| F-6 | `NON-BLOCKING` | `qa/prod-001-extraction-review-input.md:106` records `node --test scripts/check-ignored-attacks.test.mjs | PASS; 1/1`; `analyzers/github-rust-format-build-test.log` shows `1..7`, `# tests 7`, `# pass 7` | A public review record understates the guard's unit-test coverage sevenfold. Cosmetic, but it is a founder-facing evidence record | Correct the record to 7/7 |
| F-7 | `NON-BLOCKING` | `qa/prod-001-extraction-review-input.md:§2` reproduction pattern names relocation commit `91589759…`; the reviewed head is `f1d17087…` | A founder following the document's commands verifies the intermediate relocation commit rather than the head. Byte identity at the head is separately established by `metadata/t0-byte-identity.sha256` (18/18 `OK` against the reviewed snapshot), so no gap in fact — only in the instructions | Add a head-bound reproduction line (`git show f1d17087…:src/FILE.rs | sha256sum`) alongside the relocation-commit pattern |
| F-8 | `NON-BLOCKING` | `tests/bypass-rust/T0-BOUNDARY.md` and `T0-AUTHORS.md` state the PROD-000 gate completed at final head `a85e3676…` and was founder-merged through PR #90 as `8318f61e…`; the baseline commit is indeed `8318f61e…`, which is consistent. But `dgr-core/baseline/qa/` is empty in the selected snapshot and no PROD-000 independent-human review, founder review, GitHub approval reference, or cross-model record appears anywhere in the bundle | PROD-001's authorization precondition — that PR #90 merged — is asserted and internally consistent but not evidenced within this bundle. This does not affect whether the extraction is faithful, which is what the cross-model gate tests, but it must not be silently assumed | Cite the PR #90 merge commit and the independent and founder GitHub approval URLs in the founder record, or include `qa/prod-000-independent-human-review.md` and `qa/prod-000-founder-review.md` in the founder-gate bundle |
| F-9 | `NON-BLOCKING` | `analyzers/github-pr-91.json` lists two distinct run URLs per context (`…/actions/runs/3376882` and `…/runs/3376874`, ids apparently truncated in the captured JSON) while `metadata/commit-tree-identities.txt` names one `github_actions_run 33768827329`; none of the five bundled job logs contains a `GITHUB_RUN_ID` line | Minor traceability gap: the bundled logs cannot be attributed to a specific run id from the bundle alone, and it is not evident which of the two runs they came from | Record the run id (and attempt number) in each log header, or capture per-run metadata alongside the logs |

Two further evidence notes, recorded for the founder gate but not raised as findings because they do not affect any conclusion: `metadata/cargo-metadata.json` carries workspace paths rewritten to bundle-relative form (`path+file://dgr-core/reviewed`), so it was either generated from the snapshot or path-sanitised, and the bundle does not say which; and the snapshots' non-reconstruction limit means absence of untracked or non-Rust alternate copies is not establishable here, although extraction-matrix item 14(e) closes the tracked-Rust case that governs what compiles.

## Gate accounting

**Satisfied by this review**

- The non-author cross-model review gate for PROD-001, for exactly this bundle and exactly reviewed head `f1d17087d140b41750c1aeca032916bb4d2d90ae` (tree `350d1f1bdadd7b26475a44f29c981dfc00743d2a`), against baseline `8318f61eadf689f9b8a72f673cc68cd083dc7831` (tree `227949bfdb0988fa465a1fc1d540c2a447daa0f7`) and patch digest `c896f1dc3e6eeb6a447cce7a4ad71878a2ddd5bfc8ce0d005f18d92c9a24ba70`.
- Independent confirmation that the extraction is a byte-identical relocation with wiring-only changes, that the T0/T3 partition holds in both directions, that no alternate tracked route to a different enforcement body exists, and that the three analyzer legs and both required GitHub contexts ran on the merge of the actual head into the recorded baseline.
- Assessment of the four evidence limitations the manifest flagged for review: the processed-SARIF diagnostic gap (bounded, non-blocking), the cargo-deny note-count difference (bounded, non-blocking, mechanism evidenced but causation unproven), the Semgrep `--error` exit (correctly characterised, unchanged test-only result), and the selected-snapshot limit (stated, and tightly bounded for Rust by the `git ls-files` closure argument).

**Still pending — none of these is marked complete or implied by this review**

- Independent-human review by a human who authored none of the implementation or evidence. This reviewer is a model and does not satisfy that gate.
- Founder byte-level relocation, provenance and semantic-identity review, including personal verification of the nine digests and the three shared-definition regions.
- Founder disposition of every analyzer finding and diagnostic: the Semgrep `temp-dir` result, the two CodeQL raw diagnostic messages (one enumerable, one not), the CodeQL Action v3 and Node 20 deprecations, and cargo-deny's two bans notes and 53/55 license notes. **This review dispositions none of them.**
- Founder disposition of findings F-1 through F-9.
- Confirmation that at least three required analyzer engines and both required contexts pass on the actual final head after this review record and any correction commits land.
- Independent GitHub approval and founder GitHub approval of the actual final head.
- Founder-only merge. PR #91 is `OPEN` and `isDraft: true` per `analyzers/github-pr-91.json`.
- RUNTIME-003 and RUNTIME-004 remain inactive; PROD-001 confers no runtime-integration finding.

A passing verdict here satisfies only the non-author cross-model gate for this exact reviewed head and bundle. It is not independent-human review, founder disposition, GitHub approval, or merge authorization.

**Final verdict:** `PASS WITH NON-BLOCKING FINDINGS — CROSS-MODEL GATE SATISFIED`
