# PROD-000 non-author cross-model review addendum

**Verdict:** `PASS WITH NON-BLOCKING FINDINGS — CROSS-MODEL GATE SATISFIED`

This addendum supplements, and does not modify, the first non-author cross-model review at
`qa/prod-000-cross-model-review.md` (verdict `CHANGES REQUIRED`, bound to implementation commit
`40b713039a5612831df415cdd785271a7342be74`). That record remains the immutable audit record for
that commit.

## 1. Reviewer and binding

| Field | Entry |
|---|---|
| Reviewer product/model | Anthropic Claude Opus 5, with container tooling (`git`, `sha256sum`, `node` v22.22.2, `python3`) used for independent digest, tree-hash, diff and execution checks |
| Non-author attestation | The PROD-000 implementation, remediation and evidence were authored by OpenAI Codex. This reviewer is a different vendor and model family, authored none of the reviewed source, templates, diffs or evidence, and had no part in the remediation. This reviewer also authored the first cross-model review; the present addendum re-verifies every one of its findings against supplied bytes rather than restating them. |
| UTC review time | `2026-09-02T17:33:07Z` (start of inspection; completed in the same session) |
| Manifest verification | **PASS.** `sha256sum -c MANIFEST.sha256` → 453 of 453 `OK`, 0 `FAILED`, exit 0. Bundle contains 454 files (453 listed + the manifest). No file present but unlisted; no file listed but absent. Bundle archive digest matches the supplied `.sha256` sidecar (`65f900fc0eeb8035cbfc68c2ffbea3ce43216516093982cc9ab217cf034dda8d`). |
| Baseline commit | `e9c8f585809c15d2464b3d45bc2ce26d716c8673`, tree `837bef46997d5c8a9db946d2a87c1d4b6ce5aced` — **tree independently reconstructed from `dgr-core/baseline/` and matches** |
| Exact executable review commit/tree | `587585cf476431f078efe587c5dbcc052389cdad`, tree `89e5de51d23ead98d24bbbe1b4cd57db343b2dc4` — **tree independently reconstructed from `dgr-core/reviewed/` and matches**, and is the `scanned_tree` named by all three canonical scanner wrappers |
| Evidence commit/tree | `b40c9e13e3832d3df1b9290ac2e3d54830604c84`, tree `df8fb43719eb7b7176bce495f0c7e3729180bb6d` — **tree independently reconstructed from `dgr-core/evidence-head/` and matches** |
| Baseline-to-reviewed patch SHA-256 | `c08919d86a1f060cce9a05b3143140a5f011b9349f243475dad4f4ec1b40cf99` — **recomputed and matches**. All seven stored diffs verify against `metadata/stored-diffs.sha256`, and each `metadata/*.diff` is byte-identical to its in-repo `qa/prod-000-review-evidence/` counterpart. |
| Review scope and limits | See below. |

### Integrity checks performed before substantive review

1. **Manifest**: 453/453 verified (above).
2. **Lineage**: `metadata/commit-lineage.txt` contains all six required identities — baseline
   `e9c8f58…`, initial implementation `40b7130…`, original Claude-review record `9a80822…`,
   replacement source `b19f33a…`, final executable review input `587585c…`, evidence head
   `b40c9e1…` — in a single parent chain, with `20012e2…` and `7c52c91…` as later
   documentation-only descendants. `metadata/pr-identity.txt` confirms
   `DGR-AI-Labs/dgr-core#90`, branch `codex/prod-000-supervised-agent-t0`.
3. **Patch digest**: recomputed, matches (above).
4. **Post-reviewed drift**: `metadata/post-reviewed-drift.txt` lists 29 paths, all documentation or
   evidence artifacts. Independently re-derived by recursive comparison of `dgr-core/reviewed/`
   against `dgr-core/evidence-head/`: the only differences are `COMMIT.txt` (a bundle artifact, not
   a repo file), nine governance/evidence Markdown files, and the added
   `qa/prod-000-remediation-validation.md`, `qa/prod-000-review-evidence/**` and `qa/sast/prod-000-*-2026-09-02*`
   artifacts. **Zero** difference in any `*.rs`, `Cargo.toml`, `Cargo.lock`, `deny.toml`,
   `rust-toolchain.toml`, `package.json`, `.github/workflows/**` or `scripts/**` file.
5. **Snapshots**: each of the three `COMMIT.txt` files names its commit, tree and role; each
   snapshot's reconstructed git tree equals the declared tree (above). Additionally, all 30 files
   carrying `index <pre>..<post>` lines in the full patch were checked with `git hash-object`: the
   `baseline/` blob matches the pre-image object ID and the `reviewed/` blob matches the post-image
   object ID in **30 of 30** cases, with zero mismatches.
6. **Authorities**: `sha256sum -c metadata/authorities.sha256` → all three ADR files `OK`.
   `metadata/authority-source.txt` binds them to `dgr-internal` commit `4a78501…`. Amendment B's
   own text confirms it supersedes only Amendment A R5.3 authorship option (c) and leaves
   R1–R5.2a, R6, R7, every proof obligation and every non-claim unchanged.

No integrity check failed, so `REJECT` on integrity grounds does not apply.

### Scope and limits of this review

- **Read at source**: the complete baseline-to-reviewed patch; the T0 floor module, T3 adapter,
  `lib.rs`, `core_004_conformance.rs`, all eight founder-owned consumers, `attack_set.rs`,
  `fail_closed.rs`, `src/gate.mjs`, both JavaScript guard files and `ci.yml`, in both baseline and
  reviewed snapshots; all five authoring templates, `T0-AUTHORS.md`, `T0-BOUNDARY.md`, the finding
  response, the remediation validation record, all three ADR files, and every canonical
  `prod-000-final-input-*` scanner artifact including the raw Semgrep JSON and the complete CodeQL
  SARIF notification array.
- **Independently executed**: SHA-256 recomputation of every artifact relied on; git blob and tree
  reconstruction; EOL-insensitive re-derivation of the R5.1 hunk; `node --test` on the guard unit
  tests (7/7 pass); direct behavioural probing of the guard's comparison logic.
- **Not executed**: `cargo fmt`, `cargo clippy`, `cargo build`, `cargo test`, Semgrep, CodeQL and
  cargo-deny. No Rust toolchain or scanner is available in this environment and network egress is
  disabled. Those results are reviewed as artifacts bound to tree `89e5de51…`, not re-run. The
  `52 passed / 5 ignored` figure and the live libtest enumeration are therefore accepted as
  artifact evidence corroborated by source inspection, not as reviewer-reproduced results.
- **Outside the bundle**: GitHub branch-protection configuration. Workflow *membership* of both
  required jobs and of the two guard invocations is verified from `ci.yml`; whether
  `Structural / governance check` and `Rust format / build / test` are configured as *required*
  contexts is a repository setting this reviewer cannot observe.
- **Asymmetry clause**: absence of an identified defect is not proof of correctness outside the
  supplied source, patch and artifacts. This review does not establish anything about code paths,
  commits or runtime behaviour not represented in the bundle.

## 2. First-review blocking findings B1–B9

| Finding | Disposition | Evidence |
|---|---|---|
| B1 — incomplete review inputs | `RESOLVED` | Manifest 453/453 `OK`. Three complete repository snapshots whose reconstructed git trees equal the declared trees `837bef46…`, `89e5de51…`, `df8fb437…`. Full patch digest recomputed to `c08919d8…`; seven focused/full diffs verify against `metadata/stored-diffs.sha256` and are byte-identical to their in-repo copies. Lineage, PR identity, inventories and per-file critical hashes present. Every question this reviewer previously could not reach is now answerable at source. |
| B2 — evidence not bound to the reviewed head | `RESOLVED` | All three canonical wrappers name `scanned_commit 587585cf…` / `scanned_tree 89e5de51…` and record `source_materialization: git archive of scanned_commit`. The `reviewed/` snapshot reconstructs to exactly `89e5de51…`, so the scanned tree and the reviewed content are provably the same bytes rather than an author assertion. Drift `587585c…→b40c9e1…` independently re-derived as documentation/evidence only, with zero Rust, Cargo, lockfile, deny-policy, workflow or script change. Residual: the remote PR head advances when this bundle and addendum are committed; founder approval must name that later head. `metadata/pr-identity.txt` states this, and it is a process gate, not an open cross-model defect. |
| B3 — `founder_*` filename and wildcard pathspec | `SUPERSEDED BY AUTHORITY` | The rename request conflicts with active authority: **ADR-13 Amendment B §B2, item 1** requires the agent to "create `tests/bypass-rust/src/founder_before_tool_call_floor.rs`", and §B2's closing paragraph makes any unrelated change a hard stop needing a new founder decision. The filename is mandated, so the rename is withdrawn. Mitigations verified at source: line 1 of the module reads `//! {AGENT-AUTHORS}: T0 reached-boundary fail-closed floor.`, the repository convention is confirmed by `{FOUNDER-AUTHORS}` markers in the founder-authored units, and `T0-AUTHORS.md` now states that Amendment B requires this exact filename and that the marker and ownership record "prevent the `founder_` path prefix from being misread as an authorship claim". The separable wildcard defect is independently **remediated and verified**: `eight-consumer.diff` contains exactly eight `diff --git` headers — the eight consumers named in §B2 item 4 — and no header for the floor module; template 5 now cites the stored artifact instead of a `founder_*.rs` wildcard command. |
| B4 — eight-consumer wording | `RESOLVED` | `T0-AUTHORS.md` now reads "module-path rewrites in exactly eight founder-owned consumers named by Amendment B; seven files contain no other semantic change, while `founder_approval_timeout.rs` separately contains the Amendment-A R5.1 change already listed above." Template 5's scope rule states the same split and explicitly declines to certify that file's complete diff as import-only. The contradiction is gone. |
| B5 — registry-mirror assertion provenance | `RESOLVED` | Settled at source: `tests/bypass-rust/tests/attack_set.rs` is byte-identical across baseline, reviewed and evidence-head (`4ec860d8651402e4a1324ed78ea0f41a3071e3564a720253fddf7a0d51725a0d`), and the assertion was added inside the pre-existing test `atk_06_sequence_is_escalated_then_registry_derived_timeout_block` in `tests/bypass-rust/tests/core_004_conformance.rs` (+4/−1: one import widened, one `assert_eq!` added). The reviewed file digest is `29b3b06830d8d6db6cd07008575d9dfeca4dfb6767e7a476c3ed89df9e1cea09`, matching the remediation record. Both revisions of the file are in the bundle. |
| B6 — canonical direction and droppable assertion | `RESOLVED` | Canonical direction is now explicit in source: `founder_approval_timeout.rs:13` defines the private `const ATK_06_TIMEOUT_OUTCOME: RequiredOutcome = RequiredOutcome::EscalateThenDenyOnTimeout;` used by the deny arm, with `#[doc(hidden)] pub const CONFORMANCE_ATK_06_TIMEOUT_OUTCOME` as the mirror. `use crate::attack_by_id;` is gone from T0; the registry is consulted only by the T3 test. Non-droppability independently proven, not accepted: this reviewer executed `node --test scripts/check-ignored-attacks.test.mjs` → **7/7 pass**, then probed `compareRequiredActiveTests` directly — test present and active → guard passes; test **deleted** → `missing`, guard fails; test **renamed** → `missing`, guard fails; test `#[ignore]`d (listed and ignored) → `ignored`, guard fails; ignored but unlisted → both, guard fails. `ci.yml` runs both `node scripts/check-ignored-attacks.test.mjs` and `node scripts/check-ignored-attacks.mjs` inside the blocking `Rust format / build / test` job. See N14 for the one residual bound. |
| B7 — R5.1 source absent | `RESOLVED` | Both revisions of `founder_approval_timeout.rs` are supplied and were read. The raw `r5-1-timeout.diff` renders as a whole-file replacement because the file was CRLF at baseline and is LF at `587585c…`; template 5 now declares this ("yes — R5.1 plus file-wide CRLF→LF normalization"). This reviewer re-derived the true hunk with `git diff --ignore-space-at-eol` and confirmed the content change is exactly: remove `use crate::attack_by_id;` and the old `before_tool_call` import; add the `founder_before_tool_call_floor` import; add the two constants; delete the four-line registry lookup including its `None => return fail_closed_decision(GuardFault::InternalError)` arm; and change `outcome,` to `outcome: ATK_06_TIMEOUT_OUTCOME,`. Nothing else. See N13. |
| B8 — baseline floor absent | `RESOLVED` | `dgr-core/baseline/tests/bypass-rust/src/before_tool_call.rs` (172 lines) is supplied, and every digest in template 1 and template 2's baseline evidence reproduces exactly: complete file `5e44f9d6c4451bbe80c7821a6587663110b1144a6895a4dd2f8548d0e0de049d`; region `115-171` `35639394ea9d089f86b31d103027353e99e3e55522db8bf57112526d3ccfacc7`; region `126-170` `d9f2b23fb9a44422d38d8231b530ea0d739244897a9f147234e65608a1793c3f`. Semantic identity is now checkable against founder source rather than resting on the author's ledger. |
| B9 — type-classification tension | `RESOLVED` | Both `T0-AUTHORS.md` and template 1 now classify the five shapes as "pre-existing non-founder T0-by-consequence … agent-relocated/transformed", and `T0-AUTHORS.md` states they "pre-date PROD-000 and were non-founder T0-by-consequence even while physically located in the mixed-tier adapter". That is consistent with the standing catch-all rule (consequential enums and traits are T0 until the founder records a narrower classification) and with Amendment B §B1's four-way vocabulary, and it infers no narrower founder classification. The historical CORE-003 row is correspondingly restated as "Historical source … Founder-authored source provenance for the reached-boundary fault/unwind floor transformed by PROD-000". |

No blocking finding is `OPEN`.

## 3. First-review non-blocking findings N1–N12

| Finding | Disposition | Evidence |
|---|---|---|
| N1 — unreachable `GuardFault` observation | `RESOLVED` | Not dead code. `tests/bypass-rust/tests/attack_set.rs:246` matches `BeforeToolCallObservation::GuardFault { .. }` inside `assert_atk_07_fail_closed` and panics with "ATK-07 RED: the boundary exposed a raw guard fault instead of blocking". The variant is a genuine negative-conformance sentinel, and `before_tool_call.rs:42` now documents it as such. The product floor still collapses every fault to fail-closed, so the discriminant is intentionally not observable at the boundary. |
| N2 — public floor re-export | `RESOLVED` | `before_tool_call.rs:11` imports the floor privately (`use crate::founder_before_tool_call_floor::before_tool_call_floor;`) and the `pub use` at lines 12–15 lists exactly six shared types with `before_tool_call_floor` absent. Repository-wide grep confirms the only remaining public path to the function is the T0 module itself. Template 4's public-surface row for `before_tool_call_floor` remains correct and is not in tension: the function is public *in T0*, no longer re-exported through the harness. |
| N3 — missing route-around non-claim | `RESOLVED` | `founder_before_tool_call_floor.rs:81-82` now reads "It does not cover `panic=abort`, process termination, OOM abort, a hook that is never invoked, or a route around the hook." The exclusion is in the extractable source documentation that will travel with a future crate, and remains in `T0-BOUNDARY.md` lines 23 and 72. |
| N4 — literal `{AGENT-AUTHORS}` | `RESOLVED` | This reviewer's original reading was wrong and is corrected here: the token is the repository's authorship-marker convention, confirmed by `{FOUNDER-AUTHORS}` at the head of `founder_approval_timeout.rs` and `founder_fail_closed.rs`. `T0-AUTHORS.md` now states the marker's authority explicitly. Not an unsubstituted placeholder. |
| N5 — thin Semgrep ruleset | `ACCEPTED BOUNDED NON-BLOCKER` | Confirmed at artifact level: `p/rust` is 11 rules over 21 files. The response records the limitation without expanding scope or claiming a clean bill, and treats repository-specific rules as a separate hardening proposal rather than an Amendment-B prerequisite. That is the correct handling; Amendment B §B2 would make an unrelated ruleset change a hard stop. The original suggestion (mechanize the T0/T3 boundary invariants as CI rules) remains available as future hardening, outside this gate. |
| N6 — SARIF not self-binding | `ACCEPTED BOUNDED NON-BLOCKER` | Re-verified on the canonical artifact: `versionControlProvenance` is `null`, `invocations[0]` carries no `commandLine`/`workingDirectory`/timestamps, and **0 of 30** artifacts carry `hashes` despite `--sarif-add-baseline-file-info`. This is a CodeQL output limitation and is stated as such, with no claim of self-authentication. The external binding is now materially stronger than at first review: the wrapper names tree `89e5de51…`, and the reviewed snapshot reconstructs to that exact tree. |
| N7 — diagnostic array larger than the finding list | `ACCEPTED BOUNDED NON-BLOCKER` | Re-counted on the canonical SARIF: `invocations[0].toolExecutionNotifications` holds **99** entries (67 `note`, 32 unlevelled), of which 21 are "File successfully extracted", **0** are warning-level and **0** are error-level; the remainder are CodeQL consistency/diagnostic-query registrations. Both the wrapper and the finding response now direct human and founder review to the complete array rather than the nine results. Accurately bounded. |
| N8 — no JavaScript scanner coverage | `ACCEPTED BOUNDED NON-BLOCKER` | Confirmed and now explicit rather than silent. Semgrep's `paths.scanned` contains 21 `.rs` files and **zero** `.mjs` files; CodeQL ran `--language=rust`; cargo-deny is Rust SCA. Both wrappers state "no JavaScript SAST coverage is claimed". The changed JavaScript guard received `node --check`, unit tests and live libtest enumeration, which the bundle correctly declines to call JavaScript SAST. The nine `.mjs` paths still appear in the SARIF artifact table with no results and no hashes. |
| N9 — second enforcement surface (`src/gate.mjs`) | `RESOLVED` | Verified at source, not accepted on assertion: `src/gate.mjs` exports `decide()` which unconditionally throws `NotImplementedError`, is headed "Phase 0 FAILING STUB", and is not invoked anywhere in `ci.yml` (the `test:bypass` script is not wired into CI). It cannot authorize anything and is fail-closed by absence. `T0-BOUNDARY.md` now records it as "a deliberately failing Phase-0 scaffold, not an active or authoritative enforcement floor" that "neither competes with nor bypasses the Rust isolation harness". |
| N10 — `Blocked` line range | `RESOLVED` | Template 1 now records lines 64–67; line 63 is the enum header. Verified against the final file. |
| N11 — diff command instead of stored artifact | `RESOLVED` | Seven diffs are stored, digest-bound in `metadata/stored-diffs.sha256`, and byte-identical to their in-repo `qa/prod-000-review-evidence/` copies: `baseline-to-reviewed`, `full-resubmission` (identical content), `full-implementation`, `eight-consumer`, `floor-semantic-identity`, `r5-1-timeout`, `active-assertion-guard`. Template 5 cites the artifact path with "an explicit eight-file path list; no wildcard". |
| N12 — duplicate cargo-deny artifact | `RESOLVED` | Confirmed as one earlier run duplicated in the first upload set, not two engine executions. The bundle contains one `prod-000-remediation-cargo-deny-2026-09-02.txt` and one canonical `prod-000-final-input-cargo-deny-2026-09-02.txt`, and `metadata/canonical-scanner-artifacts.sha256` designates which six artifacts are canonical. |

## 4. Required source-review answers

| # | Answer | Source-grounded basis |
|---|---|---|
| 1 | `YES` | Every changed region in the patch resolves to one of the five permitted classes, and the ledgers say so truthfully. The 30-file footprint is: the new T0 module (`founder_before_tool_call_floor.rs`, +126/−0, agent-authored T0 containing agent-relocated non-founder T0-by-consequence shapes and the agent-transformed floor with identified founder-source provenance); `lib.rs` +1 registration; `before_tool_call.rs` +25/−78 (T3 facade, observation types, adapter conversion, probe invocation); eight founder-owned consumers (seven single-hunk `+1/−1` import rewrites, plus `founder_approval_timeout.rs` carrying the separately classified R5.1 change and a declared CRLF→LF normalization); `core_004_conformance.rs` +4/−1 (T3 mirror assertion); `scripts/check-ignored-attacks.mjs` +67/−11 and its test +26/−0 (T3 guard); and documentation/evidence. Independently corroborated: all eight ledger before/after SHA-256 values in template 5 match the actual baseline and reviewed files; template 1's baseline and destination file digests and template 2's two baseline region digests and destination region digest all reproduce exactly. One line-number error is recorded as N15; it does not misclassify any region. |
| 2 | `YES` | `founder_before_tool_call_floor.rs` contains zero occurrences of `EffectfulToolProbe`, `RecordingToolProbe`, `BeforeToolCallObservation`, `authorization_issued`, `effectful_invocations`, `attack_by_id`, `ATTACK_SET`, `fixtures`, and zero tool or probe invocations. Its only imports are lines 8–10: `founder_approval_store::{ApprovalStore, ReviewRequestId}`, `founder_consumption_store::ConsumptionStore`, and `crate::{DecisionContext, ProposedAction, RequiredOutcome}`. `Authorized` (lines 72–74) carries only an authorization reference. |
| 3 | `YES` | Bounded unwind: one `catch_unwind` at line 97, with `AssertUnwindSafe` wrapping a closure whose sole statement is `guard.decide(...)` at line 98. Store handling: no match arm reads either store; on `Err(_)` the function returns immediately, and lines 93–96 decline to certify store invariants for reuse. Typed fault and panic: the combined arm at lines 121–124 yields `Blocked { outcome: RequiredOutcome::FailClosed, denial_signal: "CORE-003 boundary fail-closed" }`, byte-identical to the baseline floor's values. Denial relay lines 102–108, escalation relay 109–115 (no arithmetic on `deadline` anywhere in the module), authorization relay 116–120, each moving fields through unmodified. Ordering: the module contains no policy, threshold, canonicalization or consumption logic, so it cannot perturb the Addendum-A token-bearing order; `founder_authored_guard.rs`, which owns that order and the `1_000_000` threshold, changed by a single import line and its digest matches the ledger. Semantic identity against founder source is now checkable because the baseline regions are supplied and their digests reproduce (see B8). |
| 4 | `YES` | `before_tool_call.rs` contains exactly two occurrences of `invoke(`: the trait declaration at line 18 and one call site at line 108, inside the `BeforeToolCallOutcome::Authorized` arm and before `Proceeded` is constructed. The `Blocked` (87–95) and `Escalated` (96–104) arms call only `tool.invocation_count()` and hard-code `authorization_issued: false`. The match over `BeforeToolCallOutcome` is exhaustive with three arms and no fallthrough, default or catch-all. |
| 5 | `YES` | Within this repository there is exactly one reached-boundary enforcement floor. `catch_unwind` appears three times in Rust: line 97 of the T0 module (the floor), and `attack_set.rs:282` and `:328`, which are ATK-07 conformance wrappers asserting that a panic does not escape the boundary — test scaffolding, not an enforcement path. `"CORE-003 boundary fail-closed"` appears in the T0 module (line 123) and as an expectation string in `adapter_harness.rs:112`. `before_tool_call.rs` retains no floor: zero `catch_unwind`, zero occurrences of the signal, and its only route to an outcome is delegation at lines 80–86. No `todo!()`, permissive fallback, temporary allow path or compatibility shim exists. The only other enforcement-shaped artifact, `src/gate.mjs`, unconditionally throws and is not in CI (N9). Runtime route-around is excluded from this answer by construction and remains RUNTIME-003/004 scope. |
| 6 | `YES` | Authoritative T0 outcome: `founder_approval_timeout.rs:13` private `const ATK_06_TIMEOUT_OUTCOME = RequiredOutcome::EscalateThenDenyOnTimeout`, consumed by the `TimedOut` deny arm. T3 mirror: `#[doc(hidden)] pub const CONFORMANCE_ATK_06_TIMEOUT_OUTCOME` at line 16, consumed only by `core_004_conformance.rs`, which asserts `assert_eq!(CONFORMANCE_ATK_06_TIMEOUT_OUTCOME, expected_terminal)` where `expected_terminal = attack_by_id("ATK-06").expected`, and separately asserts the registry value equals `RequiredOutcome::EscalateThenDenyOnTimeout`. Impossible branch removed: the four-line `match attack_by_id("ATK-06")` block and its `None => return fail_closed_decision(GuardFault::InternalError)` arm are deleted, and `use crate::attack_by_id;` is gone from T0. Non-droppable in required CI: the guard's `REQUIRED_ACTIVE_CONFORMANCE_TESTS` list is enforced by `compareRequiredActiveTests`, and this reviewer executed the guard's unit tests (7/7) and independently confirmed that deleting, renaming or `#[ignore]`-ing the test makes the guard exit non-zero; `ci.yml` invokes the guard inside the blocking `Rust format / build / test` job. Nothing else changed: the `Pending` arm, `matching_pending`, the stored deadline, `denial_signal: "ATK-06 approval timed out"`, the `NotFound` arm (`RequiredOutcome::FailClosed`, `"CORE-004 pending approval not found"`), the `Faulted` arm and the function signature are all byte-identical modulo line terminators, per the EOL-insensitive hunk. The only limit is that the required-context configuration itself is a GitHub setting outside the bundle. |
| 7 | `YES` | `eight-consumer.diff` contains exactly eight file headers and one hunk per file. Seven files — `founder_approval_store.rs`, `founder_authored_guard.rs`, `founder_consumption_store.rs`, `founder_fail_closed.rs`, `founder_s2_approval_store.rs`, `founder_s2_consumption_store.rs`, `founder_token_verification.rs` — are `+1/−1`, changing only `crate::before_tool_call::…` to `crate::founder_before_tool_call_floor::…`. `founder_approval_timeout.rs` contains its import rewrite plus the separately classified R5.1 change and nothing else at content level, as proven by the EOL-insensitive re-derivation. The file-wide CRLF→LF normalization is declared in template 5 and touches no enforcement expression, constant, match arm, SQL statement, store operation, returned decision or denial signal; see N13. All eight ledger before/after digests match the actual files. |
| 8 | `YES` | Byte-identical across baseline, reviewed and evidence-head: `.github/workflows/ci.yml` (`8e1619e0f212…`, and both required job names `Structural / governance check` and `Rust format / build / test` are unchanged per Amendment B §B5), `tests/bypass-rust/Cargo.toml` (`e161cbd225c1…`), `Cargo.lock` (`59c1a398c6a3…`), `deny.toml` (`8b1fbe5dece4…`), `rust-toolchain.toml` (`d52c5633ea77…`), `package.json`, `tests/bypass-rust/tests/attack_set.rs` (`4ec860d86514…`, therefore all ATK-07 test text and every active/ignored expectation unchanged) and `src/fixtures.rs`. `EXPECTED_IGNORED_ATTACKS` in the guard is unchanged and still exactly `atk_04/05/12/14/15`. `core_004_conformance.rs` changed only by widening one import and adding one `assert_eq!` inside a pre-existing test. No test was weakened, ignored, renamed or removed; no expectation was relaxed. Unrelated enforcement behaviour is unchanged: the seven import-only files and `founder_authored_guard.rs` carry no semantic edit. The `52 passed / 5 ignored` result is artifact evidence this reviewer could not re-run (see limits). |
| 9 | `YES` | In extractable source: `founder_before_tool_call_floor.rs:80-82` excludes `panic=abort`, process termination, OOM abort, a never-invoked hook and a route around the hook, and lines 93–96 decline to certify store invariants. `before_tool_call.rs:54-56` repeats the four containment exclusions. `T0-BOUNDARY.md` retains route-around, never-firing hook, missing plugin and operator bypass as RUNTIME-003/004 scope; the bounded CORE-004 claim (no real human delivery, no cross-restart waiting, no cross-instance pending state, no live non-bypassability); the prohibition on the harness becoming an alternate production path; and the statement that PROD-000 authorizes no PROD-001 extraction. Amendment B's own non-claims section states it does not establish runtime non-bypassability or authorize extraction, and does not make an agent-authored implementation founder-authored. The `state.md` claim fence and the manifest's "deliberately incomplete gates" section repeat the same limits. Nothing in the reviewed source or documents upgrades any claim. |
| 10 | `YES` | Every provenance and semantic-identity digest this reviewer could recompute reproduces exactly: template 1 baseline complete file `5e44f9d6…` and destination file `d1c98ded…`; template 2 baseline regions `35639394…` and `d9f2b23f…` and destination region `b46374c6…` (lines 77–126); all sixteen template-5 per-file before/after digests; the `core_004_conformance.rs` digest `29b3b068…`; the six baseline-identity digests in the remediation record; the patch digest `c08919d8…`; and all seven stored-diff digests. Template 2's semantic-identity line references were spot-checked against the final file at lines 77, 80–82, 93–99, 101–108, 109–115, 116–120, 121–124 and all resolve correctly, as do template 1's five destination ranges and template 3's and 4's references. One off-by-one line number is recorded as N15. |

## 5. Functional and SAST/SCA evidence

**Exact-input binding.** All three canonical wrappers record `scanned_commit 587585cf476431f078efe587c5dbcc052389cdad`, `scanned_tree 89e5de51d23ead98d24bbbe1b4cd57db343b2dc4` and materialization from a `git archive` of that commit. The binding is not merely an author assertion: the `dgr-core/reviewed/` snapshot reconstructs to git tree `89e5de51d23ead98d24bbbe1b4cd57db343b2dc4`, identical to the declared `scanned_tree` and to the tree recorded for `587585c…` in `commit-lineage.txt`. Content-to-tree identity is therefore cryptographically established; tree-to-commit identity rests on `commit-lineage.txt` and `COMMIT.txt`, which this reviewer cannot verify without the commit objects. `metadata/canonical-scanner-artifacts.sha256` verifies `OK` for all six canonical artifacts, and only `prod-000-final-input-*` was treated as canonical — the retained `prod-000-remediation-*` and `prod-000-*2026-09-01*` sets were not counted as additional engines.

**Semgrep 1.173.0 / `p/rust`.** Raw JSON: `version` 1.173.0, `results` length 1, `errors` `[]`, `paths.scanned` length 21 including `founder_before_tool_call_floor.rs` and no `.mjs` file. The single result is `rust.lang.security.temp-dir.temp-dir` at `tests/bypass-rust/tests/consumption_store.rs:19`, severity `INFO` in JSON and blocking under `--error` — disclosed accurately in the wrapper rather than reported as a pass. That file is **not** in the change footprint, so the finding does not touch a PROD-000 changed region. It is a pre-existing conformance-test helper and remains undispositioned.

**CodeQL 2.25.5 / `codeql/rust-queries@0.1.35`.** Raw SARIF: driver `CodeQL 2.25.5`, extensions `codeql/rust-queries 0.1.35+b551e89…`, `codeql/rust-all 0.2.14`, `codeql/threat-models 1.0.50`; one run; `executionSuccessful: true`; nine results, all `rust/hard-coded-cryptographic-value`, at `val_002_fixtures.rs` lines 126, 152, 169, 186, 204, 222, 339 and `val_004_fixtures.rs` lines 136, 143 — exactly the wrapper's list. Neither fixture file is in the change footprint, so no result touches a PROD-000 changed region. Coverage: 21 `.rs` artifacts, all under `tests/bypass-rust/`, including the new T0 module; **no artifact under `target/`**, which is why the earlier generated-`bindgen.rs` extraction warning is absent — the archive-based source root no longer contains build output. **Complete notification array reviewed**, not just the results: 99 `toolExecutionNotifications` (67 `note`, 32 unlevelled), comprising 21 "File successfully extracted" entries and CodeQL consistency/diagnostic-query registrations such as "Ill-formed type mention" and "PostUpdateNode should have one pre-update node"; **0 warning-level**, **0 error-level**, and 0 `toolConfigurationNotifications`. This corroborates `extraction_warning_count: 0` and `execution_error_count: 0`. The array is a diagnostic surface, not a set of security findings, and it remains for independent-human and founder review. Nine `.mjs` paths appear in the artifact table with no results and no hashes, consistent with the Rust-only scope.

**cargo-deny 0.20.2 (SCA, not code-SAST).** Exit 0; `advisories ok: 0 errors, 0 warnings, 0 notes`; `bans ok: 0/0/2 notes`; `licenses ok: 0/0/54 notes`; `sources ok: 0/0/0`. Bound to `cargo_lock_sha256 59c1a398c6a3…` and `configuration_sha256 8b1fbe5dece4…`, both of which this reviewer confirmed are the unchanged baseline values. The two bans notes and 54 license notes remain undispositioned.

**Functional gate.** The remediation record reports `cargo fmt` PASS, `cargo clippy --all-targets --locked -- -D warnings` PASS, `cargo test --all-targets --locked` PASS at 52 passed / 5 ignored / 0 failed, `npm run check:structure` PASS at 18 governance files, `node --check` on both guard files PASS, `node scripts/check-ignored-attacks.test.mjs` PASS 7/7, and `node scripts/check-ignored-attacks.mjs` PASS with the exact five-member ignored set and the required ATK-06 equality test present and active. This reviewer **independently re-executed** the two Node checks that do not require a Rust toolchain: `node --test scripts/check-ignored-attacks.test.mjs` → 7 pass, 0 fail, and direct probing of the guard's comparison logic across present, deleted, renamed, ignored and ignored-but-unlisted states, with the guard failing in every unsafe state. The Rust results were not re-run here and are accepted as artifact evidence corroborated by source inspection; `attack_set.rs` being byte-identical to baseline independently supports the unchanged active/ignored expectation set, and `EXPECTED_IGNORED_ATTACKS` is unchanged at exactly ATK-04/05/12/14/15.

**No risk disposition is made here.** Every scanner result and every CodeQL diagnostic remains open for independent-human review and founder disposition. Observations about whether a finding touches a changed region are locational facts, not acceptances.

## 6. Authorship and provenance conclusion

The classification ledger is **truthful and internally consistent** so far as it can be verified from the supplied bytes, and it now preserves Amendment B §B1's distinctions correctly:

- **Existing founder-authored source** — the CORE-002 units, the CORE-004 approval-store, timeout and guard surfaces, and the historical `BeforeToolCallAdapter::before_tool_call` floor body, whose provenance is recorded as source provenance rather than as authorship of the PROD-000 commit.
- **Agent-relocated / agent-transformed** — the reached-boundary floor, transformed from a harness observation constructor to a product outcome constructor, retaining identified founder-source provenance; and the five request, decision, fault and port shapes, classified as pre-existing non-founder T0-by-consequence types relocated and transformed by the agent.
- **Agent-authored T0** — the new module and its `lib.rs` registration, `BeforeToolCallOutcome`, the public-surface choices, the R5.1 constant and control-flow change, and all eight module-path rewrites.
- **T3** — the compatibility re-exports, observation types, adapter conversion and probe invocation, the registry-mirror assertion, and the JavaScript enumeration guard and its unit tests.
- **Founder-reviewed** — nothing. Every founder disposition field across all five templates reads `PENDING`, and both `T0-AUTHORS.md` and `T0-BOUNDARY.md` state that the replacement and guard commits are review inputs, not approval.

The Amendment-B-mandated filename `founder_before_tool_call_floor.rs` does **not** establish founder authorship. The module is agent-authored T0 under founder review, with mixed source provenance, and this addendum does not describe the completed PROD-000 result as founder-authored. No unsigned human gate is treated as complete, no T0 was authored or repaired by this reviewer, and no merge is recommended on the strength of green mechanical checks.

## 7. Scope limits

The reviewed result preserves every non-claim, and this addendum asserts none of them. PROD-000 does not establish: deployed interception or live agent non-bypassability; protection against a hook that never fires, a route around the hook, a missing plugin, or operator bypass; coverage of `panic=abort`, process termination or OOM abort; certification of either store's post-panic invariants for later reuse; real human approval delivery or real waiting; durability across restarts or cross-instance pending state; that the isolation harness is or may become a production enforcement path; or authorization for PROD-001 extraction. Rust-only SAST scope stands: no JavaScript SAST coverage is claimed, and `node --check`, unit tests and live libtest enumeration for the JavaScript guard are not JavaScript SAST. This addendum additionally claims nothing about the Rust build, lint, test or scanner runs beyond their artifact contents, nothing about GitHub branch-protection configuration, and nothing about any commit or content not represented in this bundle. Absence of an identified defect is not proof of correctness outside the supplied source, patch and artifacts.

## 8. Remaining cross-model actions

No blocking cross-model item remains. Three non-blocking items are raised by this pass and are recommended, not required, for the cross-model gate:

- **N13 — store the EOL-insensitive R5.1 hunk.** `r5-1-timeout.diff` is a raw diff and renders `founder_approval_timeout.rs` as a 63-line removal and 62-line addition because of the declared CRLF→LF normalization. Template 5 correctly states that `--ignore-space-at-eol` isolates the same semantic diff, and this reviewer confirmed it does, but the reduced form is not stored. Storing it as a digest-bound artifact alongside the raw diff would let the founder's line-by-line review of the one substantively changed founder-owned enforcement file work from a twelve-line hunk instead of a whole-file replacement. Related cosmetic note: `founder_approval_store.rs` now carries one LF line among 65 CRLF lines; harmless, and `cargo fmt --check` passes.
- **N14 — the guard binds to a test name, not to the assertion.** `REQUIRED_ACTIVE_CONFORMANCE_TESTS` makes deleting, renaming or `#[ignore]`-ing `atk_06_sequence_is_escalated_then_registry_derived_timeout_block` fail CI, which is what B6 required. It does not detect removal of the `assert_eq!(CONFORMANCE_ATK_06_TIMEOUT_OUTCOME, expected_terminal)` line from a test that keeps its name. The claim should be stated with that bound. A second anchor — for example asserting the constant inside another already-guarded test, or a source-level check that the symbol is referenced by the conformance target — would close it.
- **N15 — one off-by-one in template 5.** The ledger records the new import path in `founder_approval_timeout.rs` at line 9; the actual line is 10 (`use crate::founder_before_tool_call_floor::{GuardDecision, GuardFault};`). The pre-remediation ledger recorded 10 correctly, so this is a regression introduced while editing. All other line numbers and all sixteen per-file digests in that ledger are correct. Correct before founder line-by-line disposition.

Separately, and **not** cross-model defects, the following process gates are intentionally pending: independent-human review by a non-author human; founder line-by-line review of every consequential changed line, removed branch and public item; founder disposition of every SAST/SCA finding and of the complete CodeQL diagnostic array; confirmation that both required GitHub contexts are configured and passing on the final head; founder approval naming the exact final PR head, which will advance when this bundle and this addendum are committed; and founder-only merge of PR #90. PROD-001 extraction remains unauthorized until those complete.

## 9. Attestation

This addendum satisfies **only** the non-author cross-model review gate for PROD-000 under ADR-13 Amendment B, for executable commit `587585cf476431f078efe587c5dbcc052389cdad` (tree `89e5de51d23ead98d24bbbe1b4cd57db343b2dc4`) with evidence at `b40c9e13e3832d3df1b9290ac2e3d54830604c84` (tree `df8fb43719eb7b7176bce495f0c7e3729180bb6d`).

It is **not** independent-human review. It is **not** founder line-by-line review or founder disposition of any finding, diagnostic or provenance claim. It is **not** founder approval of any PR head and **not** merge authorization for PR #90. It does **not** authorize PROD-001 extraction. It does not disposition the one Semgrep result, the nine CodeQL results, the CodeQL diagnostic array, or cargo-deny's two bans notes and 54 license notes — all remain open. It does not convert any agent-authored or agent-transformed line into a founder-authored line, and it does not modify the first cross-model review record at `qa/prod-000-cross-model-review.md`, whose `CHANGES REQUIRED` verdict stands for commit `40b713039a5612831df415cdd785271a7342be74`.

**Reviewer:** Anthropic Claude Opus 5 (non-author; implementation and remediation authored by OpenAI Codex)
**UTC:** `2026-09-02T17:33:07Z`
**Verdict:** `PASS WITH NON-BLOCKING FINDINGS — CROSS-MODEL GATE SATISFIED`
