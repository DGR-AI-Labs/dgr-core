{
  "name": "@dgr/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "DGR (Decision-Grade Reasoning) — an experiment in runtime decision governance for AI agents. Early WIP; not production-ready.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:bypass": "vitest run tests/bypass",
    "check:structure": "node scripts/check-structure.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.7",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
