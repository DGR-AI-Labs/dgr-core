{
  "name": "@dgr/core",
  "version": "0.0.0",
  "private": true,
  "description": "An experiment in runtime decision governance for AI agents. Early work-in-progress; not production-ready.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check:structure": "node scripts/check-structure.mjs",
    "test:bypass": "node --test tests/bypass"
  },
  "//": "Phase 1 enforcement proof. The Rust bypass harness is canonical and its active conformance tests are expected green; the zero-dependency Node test script is retained only for the legacy scaffold."
}
